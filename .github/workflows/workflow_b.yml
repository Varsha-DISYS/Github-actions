name: Reusable workflow example
'on':
  workflow_dispatch:
    inputs:
      name:
        required: true
jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Running"
 
