name: Call a reusable workflow
'on':
  pull_request:
    branches:
      - main
jobs:
  call-workflow-passing-data:
    permissions:
      contents: read
      pull-requests: write
    uses: varsha-disys/github-actions/.github/workflows/workflow_b.yml@v1
    with:
      config-path: .github/labeler.yml
    secrets:
      token: '${{ secrets.SECRET_MAGIC }}'
