name: Call a reusable workflow

on:
  pull_request:
    branches:
      - main

jobs:

  call-workflow-passing-data:
    permissions:
      contents: read
      pull-requests: write
    uses: Varsha-Disys/Github-actions/.github/workflows/workflowb.yml
    with:
      token: ${{ secrets.SECRET_MAGIC }}
